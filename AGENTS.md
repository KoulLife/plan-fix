# AGENTS.md

PlanFix 프로젝트 작업 시 아래 규칙을 참고한다.

## 프론트엔드

- CSS, 색상, 테마 관련 작업을 할 때는 `tweakcn.md`를 먼저 확인한다.
- 피그마 디자인 가이드에서 정한 색상을 우선한다.
- 기존 폰트와 radius 설정은 임의로 변경하지 않는다.
- 수정 후에는 해당 화면이 깨지지 않았는지 확인한다.

## Git

- 작업 내용에 맞는 브랜치에서 작업한다.
- 커밋 메시지는 `feat:`, `fix:`, `chore:` 등의 형식을 사용한다.
- PR/MR은 생성하되 임의로 merge하지 않는다.
