spring:
  application:
    name: plan-fix

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

  jpa:
    open-in-view: false

security:
  jwt:
    secret: ${JWT_SECRET:local-development-jwt-secret-key-change-me-please}
    access-token-validity-seconds: ${JWT_ACCESS_TOKEN_VALIDITY_SECONDS:3600}
