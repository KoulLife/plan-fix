spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/planfix
    username: planfix
    password: P@ssw0rd

  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug