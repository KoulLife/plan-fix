# tweakcn 적용 가이드

PlanFix 색상은 tweakcn의 Cosmic Night 테마를 기준으로 적용한다.
최종 색상은 피그마 디자인 가이드에서 정한 보라색 계열을 우선한다.

tweakcn에서 가져온 CSS 변수는 `src/styles/globals.css`에 적용한다.

기존 `:root`, `.dark`의 색상 값은 교체해도 되지만 아래 항목은 기존 값을 유지한다.

- `--radius`
- font 설정
- 색상과 관계없는 기존 CSS

CSS 작업이나 색상 수정이 필요한 경우 이 파일을 먼저 참고한다.

적용한 뒤에는 `/login` 화면을 확인한다.

확인할 것:
- 피그마에서 정한 색상이 잘 적용됐는지
- 글자가 잘 보이는지
- 버튼, 입력창 등의 색상이 이상하지 않은지
- 로그인 화면 레이아웃이 깨지지 않았는지

## tweakcn CSS
